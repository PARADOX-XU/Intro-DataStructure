#include <iostream>
#include <string>
#include <vector>
using namespace std;

bool is_Palindrome(int test)
{
    for(int i = 0; i <= int.length()-1; i++)
    {
        if(int[i] != int[int.length()-1-i])
        {
            return false;
        }
        else
        {
            return true;
        }
    }
}
