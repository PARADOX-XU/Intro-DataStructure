#include <iostream>
#include <string>
#include <vector>
using namespace std;

bool is_Palindrome(int test){
    if std::string str = std::to_string(test)
        return true;
    else
        return false;
}
